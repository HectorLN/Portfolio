import { Dialog, DialogContent, DialogTitle } from "@mui/material";

export default function AddProject_Modal() {
  return (
    <Dialog>
        <DialogTitle>Adding a new Project</DialogTitle>
        <DialogContent>
            
        </DialogContent>
    </Dialog>
  )
}